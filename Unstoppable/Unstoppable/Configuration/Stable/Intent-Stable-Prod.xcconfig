PRODUCT_BUNDLE_IDENTIFIER = io.horizontalsystems.stable.intent
PROVISIONING_PROFILE_SPECIFIER[config=Debug-Stable-Prod] = match Development io.horizontalsystems.stable.intent
PROVISIONING_PROFILE_SPECIFIER[config=Release-Stable-Prod] = match AppStore io.horizontalsystems.stable.intent
