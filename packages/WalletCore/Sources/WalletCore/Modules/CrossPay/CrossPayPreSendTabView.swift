import SwiftUI

// Placeholder for the Cross Pay tab of PreSendView. It will get its own CrossPayPreSendViewModel that
// shares PreSendTabsViewModel.handler, so the send settings apply to Cross Pay as well.
struct CrossPayPreSendTabView: View {
    var body: some View {
        Color.clear.frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
