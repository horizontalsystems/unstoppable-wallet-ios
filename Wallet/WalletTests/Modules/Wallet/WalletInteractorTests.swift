import XCTest
import Cuckoo
@testable import Wallet

class WalletInteractorTests: XCTestCase {

//    private var mockPresenter: MockWalletPresenterProtocol!
//    private var mockUnspentDataProvider: MockUnspentDataProviderProtocol!
//    private var interactor: WalletInteractor!

    override func setUp() {
        super.setUp()

//        mockPresenter = MockWalletPresenterProtocol()
//        mockUnspentDataProvider = MockUnspentDataProviderProtocol()
//        interactor = WalletInteractor(unspentDataProvider: mockUnspentDataProvider)

//        interactor.presenter = mockPresenter

//        stub(mockUnspentDataProvider) { mock in
//            when(mock.validate(words: equal(to: words))).thenReturn(true)
//            when(mock.validate(words: equal(to: invalidWords))).thenReturn(false)
//        }
    }

    override func tearDown() {
//        mockPresenter = nil
//        mockUnspentDataProvider = nil
//        interactor = nil

        super.tearDown()
    }

    func testWalletFailed() {
//        interactor.restoreWallet(withWords: invalidWords)
//
//        verify(mockPresenter).didFailToWallet()
//        verifyNoMoreInteractions(mockLocalStorage)
    }

}
